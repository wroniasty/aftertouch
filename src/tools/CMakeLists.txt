# Game-adjacent C++ executables. Note the distinction PLAN.md section 3 draws:
# src/tools/ is this, tools/ at the repo root is repo scripting.
add_subdirectory(assetc)
add_subdirectory(tracekit)
add_subdirectory(tracegen)
add_subdirectory(tracediff)
add_subdirectory(trace_viewer)
